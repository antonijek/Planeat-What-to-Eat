# Dependencies
node_modules/

# Env / secrets
.env
.env.local
.env*.local
.env.example
scripts/.env

# Expo
.expo/
mobile/.expo/
mobile/dist/
mobile/web-build/

# Build / cache
*.tsbuildinfo
npm-debug.log*
yarn-error.log*
mobile/node_modules/

# OS / editors
.DS_Store
Thumbs.db
.vscode/

# Playwright screenshots/artifacts
.playwright-mcp/

# Generated data artifacts (regenerisani skriptama)
data/badmap.json

# Python venv (lokalni prevodioci: Argos Translate + OPUS-MT)
.venv/

# Generisani prevodi baze (regenerisani sa scripts/translate_db.js)
# mobile/src/data/translations/ NIJE ovde — app ga direktno importuje u runtime (translationContent.ts),
# mora biti u git-u ili EAS build puca (Unable to resolve module ../data/translations/*.json)
data/translations/
